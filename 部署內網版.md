# 安巡智控 v2.0：公司內網部署

本版本不使用外部 AI API，也不需要每台電腦下載瀏覽器模型。照片會送到公司內部伺服器，由 Ollama + Qwen3-VL 辨識，稽核紀錄集中保存在 PostgreSQL。

## 建議硬體

- 試用／低頻：8 核心 CPU、32 GB RAM、50 GB 可用空間。
- 建議正式環境：NVIDIA GPU 12 GB VRAM 以上、32 GB RAM、100 GB SSD。
- 預設模型：`qwen3-vl:4b`。若 GPU VRAM 足夠，可把 `.env` 改為 `qwen3-vl:8b` 提升辨識能力。

## 第一次安裝（Windows Server）

1. 安裝 Docker Desktop 或 Docker Engine，確認 Docker 已啟動。
2. 下載此專案後，將 `.env.example` 複製為 `.env`。
3. 用記事本打開 `.env`，把 `POSTGRES_PASSWORD` 改成公司專用的長密碼。
4. 在專案資料夾中以 PowerShell 執行：

   ```powershell
   powershell -ExecutionPolicy Bypass -File .\scripts\deploy.ps1
   ```

5. 第一次會下載數 GB 的 AI 模型，完成後開啟 `http://伺服器IP:8080`。

若有 NVIDIA GPU，安裝 NVIDIA 驅動與 NVIDIA Container Toolkit 後執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy.ps1 -Gpu
```

## Linux 伺服器

```bash
cp .env.example .env
# 修改 .env 密碼後
./scripts/deploy.sh
```

有 NVIDIA GPU：`./scripts/deploy.sh --gpu`

## 後續更新

在伺服器專案資料夾執行同一支部署程式即可。它會從 GitHub 取得最新版並重新建置；資料庫和模型使用 Docker volume，更新時不會刪除。

## 公司上線前必要設定

- 只允許公司 LAN／VPN 存取 8080 埠，不要暴露到公網。
- 使用公司 DNS 並由 IT 加上內網 HTTPS。
- 定期備份 Docker volume `postgres_data`。
- AI 判定僅供輔助；高風險、法規與銷項仍須由合格人員確認。
- 「清除全部資料」會刪除所有稽核紀錄，正式環境應由 IT 限制此功能。

健康檢查：`http://伺服器IP:8080/api/health` 顯示 `"ready": true` 才能開始辨識。
